{
  "name": "ui-router-react",
  "version": "0.1.0",
  "description": "State based routing for React",
  "main": "lib/ui-router-react.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development webpack-dev-server --host 0.0.0.0 --port 8000 --config webpack.example.js --history-api-fallback --content-base example",
    "build": "rimraf lib && mkdirp lib && NODE_ENV=production webpack --config webpack.build.js"
  },
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ui-router/react.git"
  },
  "dependencies": {
    "es6-shim": "^0.35.1",
    "ui-router-core": "^1.0.0-beta.1"
  },
  "devDependencies": {
    "react": "^15.0.2",
    "react-dom": "^15.0.2",
    "ts-loader": "^0.8.2",
    "typescript": "^1.8.10",
    "webpack": "^1.13.0",
    "webpack-dev-server": "^1.14.1"
  }
}
